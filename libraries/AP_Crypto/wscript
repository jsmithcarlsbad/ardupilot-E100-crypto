#!/usr/bin/env python
# encoding: utf-8

def configure(cfg):
    # AP_Crypto library files are auto-discovered by the build system
    # Ensure AP_CRYPTO_ENABLED is always set
    cfg.define('AP_CRYPTO_ENABLED', 1)

def build(bld):
    # AP_Crypto library files are auto-discovered by the build system
    pass
